#include<stdio.h>
struct complex
{
int real,img;
};
int main()
{
struct complex a,b,c;
printf("Enter a & b such that a+ib is the first complex number : ");
scanf("%d %d",&a.real,&a.img);
printf("\nEnter c & d such that c+id is the second complex number : ");
scanf("%d %d ",&b.real.&b.img);
c.real=a.real+b.real;
c.img=a.img+b.img;
printf("\nThe sum of the two complex numbers is : %d + (%di),&c.real,&c.img);
}

